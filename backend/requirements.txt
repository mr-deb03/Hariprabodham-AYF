# Smruti backend — Python 3.10 / 3.11 recommended.
fastapi
uvicorn[standard]
python-multipart          # multipart/form-data (file uploads)
python-dotenv

# Google Drive
google-api-python-client
google-auth

# Face recognition — InsightFace on onnxruntime (CPU). Light enough for
# managed hosting; far smaller footprint than TensorFlow-based stacks.
insightface
onnxruntime
opencv-python-headless
numpy

# WhatsApp
twilio

# Telegram Bot API (plain HTTP)
requests
